<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Error - <%= error.status || 500 %></title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-gray-100 flex items-center justify-center h-screen">
    <div
      class="bg-white p-10 rounded-lg shadow-lg flex flex-col items-center gap-y-6 w-full max-w-md"
    >
      <h1 class="text-4xl font-bold text-red-500">Oops!</h1>
      <pre
        class="bg-gray-900 text-green-400 p-4 rounded-md w-full text-left overflow-auto"
      >
<span class="text-red-400">Error:</span> <%= error.message || "Something went wrong. Please try again later." %>
<span class="text-yellow-400">Status Code:</span> <%= error.status || 500 %>
      </pre>
      <a
        href="/"
        class="bg-blue-500 text-white px-6 py-2 rounded hover:bg-blue-600 transition"
      >
        Go Home
      </a>
    </div>
  </body>
</html>
